services:
  app:
    build: .
    container_name: cyberhealth-app

    ports:
      - "8000:8000"

    volumes:
      - .:/var/www

    working_dir: /var/www

    command: php artisan serve --host=0.0.0.0 --port=8000
